export interface ServerSocket{
    socketID: number;
    socketMeterID: number;
    socketUserID: number;
    socketForm: string;
    socketLab: number;
    socketRow: number;
    socketCol: number;
    socketDate: string;
    socketVoltage: number;
}